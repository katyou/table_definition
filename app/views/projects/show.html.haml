.project_show_form
  %h1=@project.name

  %h2 プロジェクト説明
  .container
    = @project.description

  %h2 プロジェクトテーブル一覧
  .container
    - @project.tables.each do |table|
      =link_to table.name, project_table_path(@project, table), class: "list_form"

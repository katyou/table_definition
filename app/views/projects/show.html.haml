.project_show_form
  %h1=@project.name

  %h2 プロジェクト説明
  .container
    = @project.description

  %h2 プロジェクトテーブル一覧
  .container
    - @project.tables.each do |table|
      =link_to table.name, company_project_table_path(:company_id, :project_id, table), class: "list_form"
